name: 'Discover Instances'
description: 'Scans for all existing registration containers'

outputs:
  instances:
    description: 'JSON array of instance configurations'
    value: ${{ steps.scan.outputs.instances }}

runs:
  using: "composite"
  steps:
    - name: Scan for instances
      id: scan
      shell: bash
      run: |
        echo "🔍 Discovering all registration instances..."
        echo ""
        
        CONTAINERS=$(docker ps -a --filter "name=registration-instance-" --format "{{.Names}}" 2>/dev/null | sort)
        
        if [ -z "$CONTAINERS" ]; then
          echo "ℹ️  No existing containers found. Using default (5 instances)"
          INSTANCES='[{"name":"registration-instance-1","port":8501,"num":1},{"name":"registration-instance-2","port":8502,"num":2},{"name":"registration-instance-3","port":8503,"num":3},{"name":"registration-instance-4","port":8504,"num":4},{"name":"registration-instance-5","port":8505,"num":5}]'
        else
          echo "✅ Found existing containers:"
          echo "$CONTAINERS"
          echo ""
          
          INSTANCES="["
          FIRST=true
          
          for container in $CONTAINERS; do
            NUM=$(echo "$container" | sed 's/registration-instance-//')
            PORT=$((8500 + NUM))
            echo "  $container: Will use port $PORT"
            
            if [ "$FIRST" = "false" ]; then
              INSTANCES="${INSTANCES},"
            fi
            FIRST=false
            
            INSTANCES="${INSTANCES}{\"name\":\"$container\",\"port\":$PORT,\"num\":$NUM}"
          done
          
          INSTANCES="${INSTANCES}]"
        fi
        
        echo ""
        echo "📊 Final Configuration: $INSTANCES"
        echo ""
        echo "instances=$INSTANCES" >> $GITHUB_OUTPUT
        
        exit 0
